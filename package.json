{
  "name": "shehri-awaaz",
  "version": "1.0.0",
  "description": "Civic Issue Reporting System - Full Stack",
  "main": "api/index.js",
  "type": "module",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "install:all": "npm install --prefix frontend && npm install"
  },
  "engines": {
    "node": "20.x"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.38.0"
  }
}
