{
  "version": 2,
  "public": true,
  "buildCommand": "cd backend && npm install",
  "installCommand": "npm install && cd backend && npm install",
  "env": {
    "NODE_ENV": "production",
    "PORT": "3000"
  }
}
